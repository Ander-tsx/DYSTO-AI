# ==========================================
# G U I A   D E   E J E C U C I O N
# ==========================================
# Comando de ejecucion para MacOS / Linux (ejecutar DENTRO de la carpeta backend):
# docker run --rm -v "$PWD:/usr/src" -e SONAR_HOST_URL="http://host.docker.internal:9000" sonarsource/sonar-scanner-cli
#
# Comando de ejecucion para Windows (CMD) (ejecutar DENTRO de la carpeta backend):
# docker run --rm -v "%cd%:/usr/src" -e SONAR_HOST_URL="http://host.docker.internal:9000" sonarsource/sonar-scanner-cli
#
# Comando de ejecucion para Windows (PowerShell) (ejecutar DENTRO de la carpeta backend):
# docker run --rm -v "${PWD}:/usr/src" -e SONAR_HOST_URL="http://host.docker.internal:9000" sonarsource/sonar-scanner-cli
# ==========================================

sonar.projectKey=dysto-ai-backend
sonar.projectName=dysto-ai-backend

sonar.login=sqp_c0018dda789acb274d1b5b08e02d39c913383a60

sonar.sources=.
sonar.sourceEncoding=UTF-8

sonar.exclusions=venv/**,env/**,**/migrations/**,**/__pycache__/**,**/*.pyc,manage.py,**/tests.py,**/test_*.py,**/*.md,requirements.txt,sonar-project.properties,utils/**

sonar.python.version=3